{
  "buildCommand": "npm install --include=dev && node node_modules/vite/bin/vite.js build",
  "outputDirectory": "public/build",
  "functions": {
  "routes": [
    { "src": "/(.*)", "dest": "api/index.php" }
  ]
}
}